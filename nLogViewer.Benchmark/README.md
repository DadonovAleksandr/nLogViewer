# nLogViewer.Benchmark

Проект для тестирования производительности чтения и парсинга лог-файлов nLogViewer.

## Возможности

- ✅ Автоматическая генерация тестовых лог-файлов
- ✅ Тестирование производительности на различных объемах данных (10K, 100K, 1M записей)
- ✅ Поддержка многострочных лог-записей
- ✅ Красивый вывод результатов в консоль в виде таблицы
- ✅ Цветовая индикация производительности (зеленый/желтый/красный)
- ✅ Измерение времени выполнения, использования памяти и скорости обработки
- ✅ Сравнение производительности разных методов парсинга

## Запуск

### Windows

**Вариант 1: Через батник (самый простой)**
```cmd
run-benchmarks.bat          :: Интерактивное меню
run-benchmarks.bat --quick  :: Быстрый тест (~5 минут)
run-benchmarks.bat --full   :: Полный тест (~30 минут)
```

**Вариант 2: Через PowerShell/CMD**
```powershell
# Из корневой директории решения
dotnet run --project nLogViewer.Benchmark

# Или с параметрами
dotnet run --project nLogViewer.Benchmark -- --quick    # Быстрый тест
dotnet run --project nLogViewer.Benchmark -- --full     # Полный тест
```

### Linux/macOS

```bash
# Через скрипт
./run-benchmarks.sh          # Интерактивное меню
./run-benchmarks.sh --quick  # Быстрый тест
./run-benchmarks.sh --full   # Полный тест

# Или напрямую через dotnet
dotnet run --project nLogViewer.Benchmark
dotnet run --project nLogViewer.Benchmark -- --quick
dotnet run --project nLogViewer.Benchmark -- --full
```

### Интерактивное меню

При запуске без параметров отображается интерактивное меню:

```
═══════════════════════════════════════════════════════════════
        nLogViewer Performance Benchmark Suite
        Тестирование производительности чтения логов
═══════════════════════════════════════════════════════════════

Выберите режим тестирования:

  1. Быстрый тест (рекомендуется для разработки)
     - Тестирование с небольшими объемами данных
     - Время выполнения: ~5 минут

  2. Полный тест (для финальной проверки)
     - Полное тестирование со всеми объемами данных
     - Время выполнения: ~30 минут

  3. Парсинг (тест производительности парсеров)
     - Сравнение производительности разных методов парсинга
     - Время выполнения: ~2 минуты

Ваш выбор (1-3):
```

## Структура проекта

- **LogFileGenerator.cs** - генератор тестовых лог-файлов с реалистичным распределением типов записей
- **LogReaderBenchmarks.cs** - бенчмарки для тестирования синхронного и асинхронного чтения логов
- **MultilineLogReaderBenchmarks.cs** - бенчмарки для многострочных лог-записей
- **ParsingBenchmarks.cs** - бенчмарки для сравнения производительности методов парсинга
- **Program.cs** - точка входа с интерактивным меню и форматированием результатов

## Пример вывода

```
Результаты: LogReaderBenchmarks

┌──────────────────────────────────────┬──────────────┬──────────────┬──────────────┬──────────────┐
│ Метод                                │ Записей      │ Время        │ Память       │ Скорость     │
├──────────────────────────────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ Синхронное чтение логов              │         10K  │     45.23 ms │      8.45 MB │       221K/s │
│ Асинхронное чтение логов             │         10K  │     42.15 ms │      8.12 MB │       237K/s │
│ Синхронное чтение логов              │        100K  │    452.67 ms │     15.23 MB │       221K/s │
│ Асинхронное чтение логов             │        100K  │    421.89 ms │     14.87 MB │       237K/s │
│ Синхронное чтение логов              │       1.0M   │      4.52 s  │     18.67 MB │       221K/s │
│ Асинхронное чтение логов             │       1.0M   │      4.21 s  │     17.92 MB │       237K/s │
└──────────────────────────────────────┴──────────────┴──────────────┴──────────────┴──────────────┘

Цели производительности (Phase 1):
  • Память: < 20 MB для всех тестов
  • Скорость: > 40,000 записей/сек
```

## Цели производительности

### Phase 1 (текущие)
- **Память**: < 20 MB для всех тестов
- **Скорость**: > 40,000 записей/сек

### Цветовая индикация

#### Время выполнения
- 🟢 Зеленый: < 100ms
- 🟡 Желтый: 100ms - 1s
- 🔴 Красный: > 1s

#### Использование памяти
- 🟢 Зеленый: < 20 MB
- 🟡 Желтый: 20 MB - 100 MB
- 🔴 Красный: > 100 MB

#### Скорость обработки
- 🟢 Зеленый: > 100K записей/сек
- 🟡 Желтый: 40K - 100K записей/сек
- 🔴 Красный: < 40K записей/сек

## Формат тестовых данных

Генератор создает лог-файлы в формате NLog:

```
2025-01-15 10:30:45.123|INFO|Application.Core|Process:1234|Thread:5|Application started successfully
2025-01-15 10:30:45.223|DEBUG|Cache.Service|Process:1234|Thread:7|Cache miss for key: 42
2025-01-15 10:30:45.323|ERROR|Database.Manager|Process:1234|Thread:3|Failed to connect to database: timeout
  Stack trace:
    at System.Component.Method() in C:\Source\File.cs:line 123
    at Application.Handler.Process() in C:\Source\Handler.cs:line 456
```

### Распределение типов записей
- Fatal: 1%
- Error: 4%
- Warning: 10%
- Info: 45%
- Debug: 25%
- Trace: 15%

## Зависимости

- BenchmarkDotNet 0.14.0
- NLog 5.2.6
- .NET 9.0

## Примечания

- Тестовые файлы создаются во временной директории системы (`Path.GetTempPath()`)
- После завершения тестов файлы автоматически удаляются
- Для точных результатов рекомендуется запускать бенчмарки в Release режиме
- Закройте другие приложения перед запуском полного теста для минимизации шума в измерениях
